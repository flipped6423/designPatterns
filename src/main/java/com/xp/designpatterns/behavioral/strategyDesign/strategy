策略模式：
策略模式需要一个策略接口，不同策略实现不同的实现类，在具体业务环境中仅持有该策略接口，根据不同的场景选则不同的实现类

优点：
1.干掉繁琐的if、switch判断逻辑
2.代码优雅、可复用、可读性好
3.符合开闭原则、扩展性好、便于维护

缺点：
1.策略如果很多的话，会造成策略类膨胀
2.使用者必须清除所有策略类及其用途

策略模式在JDK中的应用：
1.线程池中的拒绝策略
2.比较器